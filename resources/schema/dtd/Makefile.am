## Process this file with automake to produce Makefile.in
dtddir = $(schemadir)/dtd

nobase_dist_dtd_DATA = schema.dtd \
						  unit_data.dtd \
						  unit_log.dtd

uninstall-local:
	-rm -r $(dtddir)
